<!DOCTYPE html>
<!--[if lt IE 7]> <html class="lt-ie9 lt-ie8 lt-ie7" lang="en"> <![endif]-->
<!--[if IE 7]>    <html class="lt-ie9 lt-ie8" lang="en"> <![endif]-->
<!--[if IE 8]>    <html class="lt-ie9" lang="en"> <![endif]-->
<!--[if gt IE 8]><!--><html lang="en"><!--<![endif]-->

<head>
    <meta charset="utf-8">

    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Bootstrap Stylesheet -->
    <link rel="stylesheet" href="{{ app.request.baseurl }}/backend/bootstrap/css/bootstrap.min.css" media="screen">

    <!-- Main Layout Stylesheet -->
    <link rel="stylesheet" href="{{ app.request.baseurl }}/backend/assets/css/error.css" media="screen">

    <title>{{ pageTitle }}</title>

</head>

<body>
{% block pageContent %}
{% endblock %}


<!-- Core Scripts -->
<script src="{{ app.request.baseurl }}/assets/js/libs/jquery-1.8.2.min.js"></script>
<script src="{{ app.request.baseurl }}/backend/assets/js/libs/jquery.placeholder.min.js"></script>

<script>
    function support() {
        var vendorPrefixes = "O Ms Webkit Moz".split( ' ' ),
                i = vendorPrefixes.length, support = true,
                divStyle = document.createElement('div').style;

        while( i-- ) {
            for(var a = 0, support = true; a < arguments.length; a++) {
                support = (vendorPrefixes[ i ] + arguments[ a ] in divStyle);
            }

            if( support ) return true;
        }

        return false;
    }

    $(document).ready(function() {
        support( 'Animation' ) && $('#error-digits > span').each( function(i, span) {
            $(span).addClass('bounceInDown');
        });
    });

</script>

</body>

</html>